package me.evrooij.services;

import org.junit.After;
import org.junit.Before;
import org.junit.Test;

import static me.evrooij.util.NetworkUtil.POST;
import static me.evrooij.util.NetworkUtil.request;
import static org.junit.Assert.*;

/**
 * @author eddy on 1-1-17.
 */
public class AccountServiceTest {
    @Before
    public void setUp() throws Exception {

    }

    @After
    public void tearDown() throws Exception {

    }

    @Test
    public void testAddFriend() throws Exception {
//        Adds an account to friends of an account.

        int accountId;

//        String json = request(POST, String.format("/student?name=%s&age=%s", name, String.valueOf(age)));
//        Student student = new Gson().fromJson(json, Student.class);
//
//        assertEquals(name, student.getName());
//        assertEquals(age, student.getAge());
    }

}